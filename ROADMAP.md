# ロードマップ

無料版(サーバーレス)で実現できる範囲を突き詰め、公開後に有料ニーズが見えたらサーバーを導入する、という段階的な方針。

## 無料版（サーバーレスを突き詰める）

すべてブラウザ内保存（IndexedDB）で完結させる。「サーバー不要・1ファイルで動く」という強みは維持する。ただし履歴は1端末に閉じる。

| # | 機能 | 補足 |
|---|---|---|
| 1 | 名簿のバージョン管理 | CSVを取り込むたびにスナップショット保存。前回との差分（追加/削除/変更された人）を表示 |
| 2 | 送付履歴の記録 | 生成のたびに「いつ・誰に・どのテンプレートで」を記録し、各行に「前回送付日」を表示 |
| 3 | ステータス管理 | 送付済み/宛先不明で返送/転居確認中 などのフラグを名簿側に保持 |
| 4 | 対象者フィルタ | ステータスや条件で絞り込んでから一部だけ生成（全件ではなく必要な人だけ） |
| 5 | 条件分岐テンプレート | 列の値（契約タイプ・クラス等）に応じて帳票要素セットを自動切替 |
| 6 | ユニークID自動採番 | 使い捨てトークン列を生成時に自動発行（更新確認リンクなど向け） |
| 7 | 履歴のバックアップ書き出し/読み込み | 名簿＋送付履歴をJSONでエクスポート/インポート。将来サーバー版へ引っ越すときの移行経路になる、意図的な布石 |
| 8 | 外部CSVの列名マッピング | 賃貸管理システム等、既存システムのエクスポートをそのまま読み込めるように |

## 有料版（サーバー導入後に初めてできること）

「複数人」「複数端末」「消えない」が必要になった時点がライン。

| # | 機能 | 理由 |
|---|---|---|
| 1 | 複数人・複数端末での共有履歴 | ブラウザローカルでは1台に閉じるため、チーム利用には必須 |
| 2 | クラウドでの名簿保管 | 端末紛失・ブラウザデータ消去に耐える |
| 3 | チーム内の権限管理 | 担当者ごとに閲覧/生成権限を分ける |
| 4 | 監査ログ | 誰がいつ何を生成したか。社労士事務所や管理会社などコンプライアンス業種向け |
| 5 | 外部連携（Googleスプレッドシート等） | 名簿を随時同期、二重入力をなくす |
| 6 | 郵送・メール自動送信連携 | 生成したPDFを郵送代行APIやメール添付で自動配信 |
| 7 | 配達ステータスの自動反映 | 郵便局・配達員からのフィードバックをフォームで受け取り返送フラグを自動更新 |
| 8 | 進捗ダッシュボード | 複数担当者向けの送付状況の可視化 |

無料版の7番（エクスポート/インポート）を最初から入れておくことで、有料版のサーバーに移行する際もデータが引き継げる。

## 想定ユーザー像

契約者・会員が多く、名簿ベースで個別対応の紙を配る業務全般。塾、賃貸管理会社、マンション管理組合、保険代理店、士業（社労士・税理士）、フィットネスジム、クリニック、学校・PTA、イベント/展示会運営など。
